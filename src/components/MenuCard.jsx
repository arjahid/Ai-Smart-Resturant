import React from 'react';
import NavBar from './NavBar';

const MenuCard = () => {
    return (
        <div>
            <NavBar></NavBar>
            wellcome to menu page
        </div>
    );
};

export default MenuCard;